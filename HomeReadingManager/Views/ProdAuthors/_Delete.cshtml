@model HomeReadingManager.ViewModels.AuthorEditModel

@{
    ViewBag.Title = "Delete";
}

@using (Html.BeginForm("DeleteDeleteConfirmed", "ProdAuthors", FormMethod.Post))
{
    @*@Html.AntiForgeryToken()*@
    <div class="modal-body">
        <h2>Delete Author</h2>

        <h3>Are you sure you want to delete this author?</h3>
        <div>
            <h4>@Html.DisplayFor(m => Model.Title)</h4>
            <hr />
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.Author)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Author)
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.Role)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Role)
                </dd>
            </dl>
        </div>
    
            @*@using (Html.BeginForm())
    {*@
   
        

        <div class="form-actions no-color">
            <button class="btn" type="button" data-dismiss="modal">Cancel</button>
            <input type="submit" value="Delete author" class="btn btn-danger" /> 
           
        </div>
    </div>
}

